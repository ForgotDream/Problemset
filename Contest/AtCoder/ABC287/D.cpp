#include <bits/stdc++.h>

using namespace std;

const int N = 1e3 + 50;
string s, t;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    cin >> s >> t;

    for (int i = 0; i < t.length(); i++) ;
    return 0;
}