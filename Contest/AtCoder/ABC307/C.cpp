#include <bits/stdc++.h>

using i64 = long long;

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  std::array<int, 3> h, w;
  
  std::cin >> h[0] >> w[0];
  
  for (int i = 0; i < h[0]; i++) {

  }

  return 0;
}