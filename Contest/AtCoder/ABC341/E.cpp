#include <bits/stdc++.h>

using i64 = long long;
using u32 = unsigned;

constexpr int N = 5e5 + 50;

int n, m;
char s[N];

void solve() {
  std::cin >> n >> m >> (s + 1);

  for (int opt, l, r; m; m--) {
    std::cin >> opt >> l >> r;
    
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  // std::cin >> t;
  while (t--) solve();

  return 0;
}
