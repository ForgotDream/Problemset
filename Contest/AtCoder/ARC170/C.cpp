#include <bits/stdc++.h>

using i64 = long long;
using u32 = unsigned;

constexpr int N = 5050, mod = 998244353;

int n, m;
int f[N][N];

void solve() {
  std::cin >> n >> m;
  for (int i = 1; i <= n; i++) {
    for (int j = 0; j <= n; j++) {
      
    }
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  // std::cin >> t;
  while (t--) solve();

  return 0;
}
