#include <bits/stdc++.h>

using i64 = long long;

i64 n, f[20][200];

i64 dfs(int pos, int sum, bool lim) {
  if (!pos) return 1;
}

void solve() {
  std::cin >> n;
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  // std::cin >> t;
  while (t--) solve();

  return 0;
}
