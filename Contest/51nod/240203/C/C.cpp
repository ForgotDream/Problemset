#include <bits/stdc++.h>

using i64 = long long;
using u32 = unsigned;

int n, m, k;

void solve() {
  std::cin >> n >> m >> k;
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  // std::cin >> t;
  while (t--) solve();

  return 0;
}
