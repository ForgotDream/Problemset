#include <iostream>

using i64 = long long;

constexpr int N = 
void solve() {
}

int main() {
  // freopen("firstsnow.in", "r", stdin);
  // freopen("firstsnow.out", "w", stdout);
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) solve();
  return 0;
}
