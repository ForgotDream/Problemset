/**
 * @file
 * @author  ForgotDream
 * @brief
 * @date    2023-10-02
 */
#include <iostream>
#include <set>

using i64 = long long;

constexpr int N = 1e6 + 50;
int q;
std::set<int> col[N];
struct BIT {
  
} bit;
void solve() {
  std::cin >> q;
  while (q--) {
    char opt;
    int u;
    std::cin >> opt >> u;
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) solve();
  return 0;
}
