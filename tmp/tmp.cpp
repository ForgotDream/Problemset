/*
 * @file    
 * @author  ForgotDream
 * @brief   
 * @date    2023-12-28
 */
#include <bits/stdc++.h>

using i64 = long long;

constexpr int N = 1.5e5 + 50;
int n, q, A;
std::vector<int> adj[N / 1000];

void solve() {
  std::cin >> n >> q >> A;
  for (int i = 1, u, v, w; i < n; i++) {
    std::cin >> u >> v >> w;
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int t = 1;
  // std::cin >> t;
  while (t--) solve();

  return 0;
}
