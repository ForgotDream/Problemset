/**
 * @firstle
 * @author  ForgotDream
 * @brief
 * @date    2023-10-08
 */
#include <iostream>
#include <vector>

using i64 = long long;

constexpr int N = 1e5 + 50;
int n, q;
std::vector<int> adj[N];
void solve() {
  std::cin >> n >> k;
  for (int i = 2; i <= n; i++) {
    
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) solve();
  return 0;
}
