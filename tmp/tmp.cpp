/**
 * @file
 * @author  ForgotDream
 * @brief
 * @date    2023-10-12
 */
#include <iostream>

using i64 = long long;

constexpr int N = 2e5 + 50;
int n, q, a[N];
void solve() {
  std::cin >> n >> q;
  for (int i = 1; i < n; i++) std::cin >> a[i];
  while (q--) {
    int opt;
    std::cin >> opt;

  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) solve();
  return 0;
}
