/**
 * @file
 * @author  ForgotDream
 * @brief
 * @date    2023-10-09
 */
#include <iostream>

using i64 = long long;

constexpr int N = 550;
int n, m, k;
char a[N][N];
void solve() {
  std::cin >> n >> m >> k;
  for (int i = 1; i <= n; i++) std::cin >> (a[i] + 1);
  for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= n; j++) {
      
    }
  }
}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);
  int t = 1;
  // std::cin >> t;
  while (t--) solve();
  return 0;
}
