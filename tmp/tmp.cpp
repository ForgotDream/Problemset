/**
 * @file    
 * @author  ForgotDream
 * @brief   
 * @date    2023-05-10
 */
#include <bits/stdc++.h>

using i64 = long long;

signed main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(0);

  int n, m;
  std::cin >> n >> m;

  for (int i = 0; i < n - 1; i++) {
    int u, v;
    std::cin >> u >> v;
  }

  std::vector<int> w(n);
  for (int i = 0; i < n; i++) {
    std::cin >> w[i];
  }

  for (int i = 0; i < m; i++) {

  }

  return 0;
}