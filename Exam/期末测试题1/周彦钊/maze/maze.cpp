#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const int N = 550;
int n, m;
char ch[N][N];
ll f[N][N];

void solve() {
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= m; j++)
			
	return;
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	cin >> n >> m;
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= m; j++)
			cin >> ch[i][j];
	solve();
	return 0;
}