#include <bits/stdc++.h>

using i64 = long long;

int main() {
  freopen("field.in", "r", stdin);
  freopen("field.out", "w", stdout);

  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  std::cout << 5 << "\n";

  return 0;
}